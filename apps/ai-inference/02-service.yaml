apiVersion: v1
kind: Service
metadata:
  name: inference-service
  namespace: ai-inference
  labels:
    app: inference-service
spec:
  type: ClusterIP
  selector:
    app: inference-service
  ports:
    - name: http
      port: 80
      targetPort: 8000
